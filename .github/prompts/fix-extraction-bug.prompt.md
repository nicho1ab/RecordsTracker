Create a failing fixture or regression test for the extraction bug first. Then make the smallest parser change needed. Identify the root cause and update governance, testing, fixture, connector, or workflow documentation if a missing or unclear rule allowed the bug. For fixture hash failures, verify `.gitattributes` line endings and Git-normalized fixture bytes. Update user docs only if user-visible behavior changes. Run or list validation commands.
