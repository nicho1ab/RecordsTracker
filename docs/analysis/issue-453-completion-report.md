# Issue #453 source-to-screen coverage completion report

## Completion decision

Issue #453's deterministic v1 coverage producer is integrated with the
read-only Issue #477 operator consumer and is ready for hosted acceptance at the
repository boundary. This completion statement is limited to the governed
package contract and automated local hosted evidence. It is not a live-source,
statewide-completeness, freshness, deployment, or QNAP claim, and it performs no
GitHub lifecycle action.

The stable consumer boundary proves the producer's real output rather than a
dashboard-owned approximation:

- schema ID `issues-453-477-coverage-report-v1` and contract version `1.0.0`;
- deterministic report and facility-entry identities;
- canonical JSON, LF JSONL, and LF aggregate CSV serialization;
- exact artifact media types and producer aggregate CSV header;
- field/stage coverage, terminal classifications, operational aggregates,
  facility index, job index, release assessment, and reconciliation objects;
- controlled source-layout values `supported`, `unsupported`, `unavailable`,
  and `not_applicable`; and
- fail-closed version, hash, unavailable, and reconciliation handling.

The consumer imports `ccld_complaints.source_to_screen_coverage`, not the audit
implementation. The producer remains responsible for schema validation,
reconciliation, classification, and counts. Legacy Issue #477 packages are
isolated behind explicit fixture mode and cannot substitute for a real package
in production-style reads.

## Preserved boundaries

Issue #490 remains unchanged: seven existing program-specific sources are
retained only for the evaluated scope; every statewide candidate remains
inactive; no statewide completeness baseline or cadence is approved; and raw
`733` remains unresolved without an invented facility-type label.

Retention remains `pending_policy`. No cleanup automation is authorized. The
integration adds no retrieval, import, retry, apply, cancel, resume, backfill,
job mutation, persistence, database write, schema, migration, schedule,
deployment, or remote-host behavior.

## Acceptance evidence

The final automated evidence packet is generated by
`scripts/capture-operator-coverage-dashboard-evidence.ps1`. Its manifest records
the real producer report ID, schema ID, generation mode, artifact media types,
aggregate CSV header, facility/job row counts, and controlled source-layout
values. The same packet captures summary, facility, job, CSV, responsive,
keyboard, 200% reflow, print, empty, partial, unavailable, interrupted,
hash-failed, reconciliation-failed, version-mismatch, raw-`733`, and adjacent
keyset states using only the exact GET allowlist.

Final automated evidence:

- packet:
  `data/processed/ui-evidence/20260719-080223Z-issues-453-477-operator-coverage/`;
- zip:
  `data/processed/ui-evidence/20260719-080223Z-issues-453-477-operator-coverage.zip`;
- 23 captures and 590 passing assertions, with zero failed assertions or
  unexpected route statuses;
- 97 packet files totaling 13,054,349 bytes;
- zip size 6,234,981 bytes; and
- zip SHA-256
  `f22142cce46c50ad2c69613b26fcfe591f5320d1eb791e021facc43bb78d6a66`.

Generated evidence remains under ignored `data/processed/ui-evidence` and is
not committed by default.

## Final validation record

- focused producer/dashboard/evidence/line-ending regressions: `31 passed`;
- complete local pytest suite: `1,223 passed, 2 skipped`;
- repository-wide Ruff: passed;
- repository-wide mypy: passed for 69 source files;
- documentation validation: passed;
- documentation-quality tests: `11 passed`;
- no-secrets scan: passed;
- `git diff --check`: passed;
- operator JSONL/CSV and Issue #490 CSV LF audit: passed; and
- intentional hash-failure fixture mismatch audit: passed.

The worktree did not contain a local `.venv`; validation used the existing
project virtual environment from the synchronized base checkout with this
worktree's sources. No validation result was weakened or bypassed.
