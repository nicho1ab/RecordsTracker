# Known limitations

- This is an inactive supplementary observation, not a replacement, canonical
  source, source-of-record determination, or production activation.
- The publisher identifies the license as Creative Commons Attribution but does
  not publish an exact version or dataset-specific attribution sentence. The
  repository preserves Andrew's approved provisional attribution as a
  product-owner risk decision, not a legal conclusion.
- Publisher freshness, update cadence, maintainer, steward, and system-of-record
  status remain unverified. Catalog timestamps alone do not establish content
  change.
- Query retrieval is the only live observation authorized here. Export and
  query are not treated as interchangeable; prior evaluation-only export,
  replica, redirect, Azure, signed, and opaque paths remain prohibited.
- `FAC_NBR` is non-unique grouping/matching metadata. Source-row identity is
  snapshot plus `ObjectId`.
- `CLIENT_SERVED` and `FAC_CO_NBR` remain raw only. Raw `TYPE`, `STATUS`, and
  `733` never receive invented labels.
- No source precedence, canonical reconciliation, reviewer integration,
  backfill, scheduled refresh/retry/recovery, operator mutation, deployment,
  QNAP, Cloudflare, production PostgreSQL, or Hosted acceptance is implemented.
